name: "platex Action"
description: "Compile tex file with platex"
inputs:
  who-to-greet:
    description: "Who to Greet"
    required: true
    default: "World"
outputs:
  time:
    description: "The time we greeted you"
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{inputs.who-to-greet}}
